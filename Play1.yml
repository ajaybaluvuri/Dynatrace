---
- name: starting process
  hosts: localhost
  become: yes
  become_user: root
  vars:
    - service_name: ngnix
    - type: restarted
 
  tasks:
    - name: Install Nginx
      apt:
        name: nginx
        state: present
      become: yes
    - name: Restart Nginx
      ansible.builtin.service:
        name: "{{ service_name }}"
        state: "{{ type }}"
